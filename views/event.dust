{> layout /}

{< mainContent}
	<div class="page-header">
		<h1>Bet Victor Live Event</h1>
	</div>
	<p class="lead">{event.title}</p>
	<p>{event.meeting}</p>
	
	<p>{event.status} {event.score}</p>
	<p><a href="/sports/{sport.id}/events/{event.id}/outcomes">See Outcomes</a></p>
{/ mainContent}